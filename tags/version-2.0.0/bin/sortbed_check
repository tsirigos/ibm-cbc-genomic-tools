#!/bin/tcsh 

##
## USAGE: sortbed_check [-i] <BED-FILE>
##

if ($#argv > 1) then
  grep '^##' $0
  exit
endif

if ($1 == "") then
  sort -c +0 -1 +5 -6 +1 -2n
else if ($1 == "-i") then
  sort -c +0 -1 +1 -2n
else
  exit
endif

