#!/bin/tcsh 

##
## USAGE: sortreg_check [-i] <REG-FILE>
##

if ($#argv > 1) then
  grep '^##' $0
  exit
endif

if ($1 == "") then
  sort +1 -3 +3 -4n -c
else if ($1 == "-i") then
  sort +1 -2 +3 -4n -c
else
  exit
endif

